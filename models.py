"""Models for the {{ app_name }} app."""
from django.db import models, transaction
from django.db.models import Q
from django.utils.translation import ugettext_lazy as _

# from core.mixins import Archivable, Timestamped

# Please set the fields verbose_name always and db_index & related_name if needed.
# Please set the model's Meta class options: ordering, verbose_name and verbose_name_plural.
# Set the objects member to a qustom QuerySet if you want custom methods.
